# Example connector (replace with real API calls)


def sample_action(user: str, params: dict) -> dict:
"""Pretend to perform an action on an external system and return a result."""
# Example: update a CRM record or fetch customer data
return {"status": "ok", "user": user, "params": params}
